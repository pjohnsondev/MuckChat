package muck.core.character;

public class NPCStateNone implements INPCState {

    @Override
    public void handle() {
        // Do nothing - null pattern - default behaviour instead of using a null type
    }
}
